# Secrets / local config
.env

# Persistent Playwright profile
.pw_profile/

# Bot output (applied / skipped / manual-required JSONL logs default
# to logs/jobright/, so the broad logs/ ignore covers them).
logs/
bid/

# Vendored cache files written by steps/* (keyed on local DOCX
# mtimes, not portable, regenerated on every run).
steps/_resume_cache.json
steps/_judge_vocab_cache.json

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
.env.local

# IDE
.vscode/
.idea/
